const RestaurantCategory = () => {

    return (
        <div>
            {/* header */}
            <div className="w-6/12 m-auto my-4 justify-between bg-gray-50 shadow-lg p-4">
                {/*<span className="font-bold text-lg">{data?.title} ({data?.itemCards?.length || 0})</span>*/}
                {/*<span>down</span>*/}
                Recommends
            </div>
            {/* accordion body */}
            <div>
                rest category
            </div>

        </div>
    );
};

export default RestaurantCategory;
